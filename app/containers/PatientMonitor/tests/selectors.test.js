// import { fromJS } from 'immutable';
// import { makeSelectPatientMonitorMobileDomain } from '../selectors';

// const selector = makeSelectPatientMonitorMobileDomain();

describe('makeSelectPatientMonitorMobileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
