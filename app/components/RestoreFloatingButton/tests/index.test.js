// import React from 'react';
// import { shallow } from 'enzyme';

// import RestoreFloatingButton from '../index';

describe('<RestoreFloatingButton />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
